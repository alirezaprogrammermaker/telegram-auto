[project]
name = "telegram-admin-bot"
version = "0.1.0"
description = "Telegram admin control-plane bot on Cloudflare Python Workers"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
