# Auto-scaffolded by admin-bot / scripts/scaffold_account.py
# Linkdir discovery batches (search/snowball/rerank) — not a long-running bot.
# Session secret: TELEGRAM_SESSION_B64_LINKDIR5
# Profile: config/accounts/linkdir5.json

name: run-linkdir-linkdir5

on:
  schedule:
    - cron: "2 6 * * *"
    - cron: "9 11 * * *"
    - cron: "16 16 * * *"
    - cron: "23 21 * * *"
  workflow_dispatch:
    inputs:
      steps:
        description: "Comma steps: search,snowball,rerank"
        required: false
        type: string
        default: ""

permissions:
  contents: read

jobs:
  linkdir5:
    uses: ./.github/workflows/run-linkdir.yml
    with:
      account_id: linkdir5
      session_name: linkdir5
      steps: ${{ inputs.steps || '' }}
    secrets:
      API_ID: ${{ secrets.API_ID }}
      API_HASH: ${{ secrets.API_HASH }}
      TELEGRAM_SESSION_B64: ${{ secrets.TELEGRAM_SESSION_B64_LINKDIR5 }}
      ADMIN_BOT_BRIDGE_URL: ${{ secrets.ADMIN_BOT_BRIDGE_URL }}
      ADMIN_BOT_BRIDGE_TOKEN: ${{ secrets.ADMIN_BOT_BRIDGE_TOKEN }}
