# Auto-scaffolded by admin-bot / scripts/scaffold_account.py
# Linkdir discovery batches (search/snowball/rerank) — not a long-running bot.
# Session secret: TELEGRAM_SESSION_B64_LINKDIR4
# Profile: config/accounts/linkdir4.json

name: run-linkdir-linkdir4

on:
  schedule:
    - cron: "1 6 * * *"
    - cron: "8 11 * * *"
    - cron: "15 16 * * *"
    - cron: "22 21 * * *"
  workflow_dispatch:
    inputs:
      steps:
        description: "Comma steps: search,snowball,rerank"
        required: false
        type: string
        default: ""

permissions:
  contents: read

jobs:
  linkdir4:
    uses: ./.github/workflows/run-linkdir.yml
    with:
      account_id: linkdir4
      session_name: linkdir4
      steps: ${{ inputs.steps || '' }}
    secrets:
      API_ID: ${{ secrets.API_ID }}
      API_HASH: ${{ secrets.API_HASH }}
      TELEGRAM_SESSION_B64: ${{ secrets.TELEGRAM_SESSION_B64_LINKDIR4 }}
      ADMIN_BOT_BRIDGE_URL: ${{ secrets.ADMIN_BOT_BRIDGE_URL }}
      ADMIN_BOT_BRIDGE_TOKEN: ${{ secrets.ADMIN_BOT_BRIDGE_TOKEN }}
